export interface Pelicula{
    id?: string  | null,
    nombre: string  | null,
    tipoPelicula: string | null,
    year: number | null,
    stock: number | null,
    precio: number | null,
    url : string | null,
}